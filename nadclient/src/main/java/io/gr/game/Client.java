package io.gr.game;

import java.net.InetSocketAddress;

import io.nadron.client.NettyUDPClient;
import io.nadron.client.app.Session;
import io.nadron.client.app.impl.DefaultSession;
import io.nadron.client.handlers.netty.UDPPipelineFactory;

public class Client {
	
	public static void main(String[] args) {
		
//		NettyUDPClient client =  new NettyUDPClient(new InetSocketAddress("192.168.", 18090), new UDPPipelineFactory(new InetSocketAddress("192.168.", 18090)), null);
//		
//		Session session = new DefaultSession();
//		
//		client.connect(session, datagramChannel);
		
	}
	
}
